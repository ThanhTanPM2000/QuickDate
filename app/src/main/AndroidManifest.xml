<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quickdate">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:largeHeap="true"
        android:hardwareAccelerated="false"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuickDate">
        <activity android:name=".activities.InterestsAct" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.TypeAct" />
        <activity android:name=".activities.DoneAct" />
        <activity android:name=".activities.SignUpAct" />
        <activity android:name=".activities.SelectGenderAct" />
        <activity android:name=".activities.SwipeAct" />
        <activity android:name=".activities.ForgotPasswordAct" />
        <activity android:name=".activities.LoginAct" />
        <activity android:name=".activities.BioPhotosAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
    </application>

</manifest>